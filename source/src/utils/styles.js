export const dFlexColumn = {
  display: "flex",
  flexDirection: "column",
};
export const dFlexRow = {
  display: "flex",
  flexDirection: "row",
};
export const TPflexGrow = (flexSize) => ({ flexGrow: flexSize });
export const width = (size) => ({ width: size });
